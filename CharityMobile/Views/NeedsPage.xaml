<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="CharityMobile.Views.NeedsPage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="{Binding Title}"></Label>
            <ActivityIndicator IsRunning="True" IsVisible="{Binding IsLoading}" WidthRequest="200" VerticalOptions="Center"/>
            <ListView ItemsSource="{Binding NeedCollection}">
                
                <ListView.Behaviors EventName="ItemTapped" Command="{Binding NavigationCommand}" CommandParameter="11">
                    
                </ListView.Behaviors>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Name}" 
                                  Detail="{Binding Status}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>